#include <iostream>
#include <cstdio>
#include <cstring>

using namespace std;

int digit_count[10];

int main(){
	freopen("7203-At_most_twice_in.txt", "r", stdin);

	int N;
	long c;
	while(c != EOF){
		cin >> c;
		cout << c << endl;
	}
	/*
	while((c = getchar()) != EOF){
	
		memset(digit_count, 0, sizeof(digit_count));

		cout << (char)c << endl;
	//	printf("%c", (char)c);
	}
	*/

	return 0;
}
